## Summary

<!-- What changed and why -->

## Checklist

- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run test:coverage` passes with 100% coverage maintained
- [ ] `npm run build` passes
- [ ] Docs updated if behavior changed (`README.md`, `docs/`, `skills/fetch-rig/`)
